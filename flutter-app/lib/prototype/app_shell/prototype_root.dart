import 'package:flutter/material.dart';

import 'variant_c/variant_c.dart';

/// Top-level prototype host — variant C only.
class PrototypeRoot extends StatelessWidget {
  const PrototypeRoot({super.key});

  @override
  Widget build(BuildContext context) => const VariantCCarousel();
}
